<%# Result insight (#5): a second-angle read on the same frozen snapshot —
    downside, dispersion, or the "why" — branched so it never contradicts the
    chart. Renders a Monte::Copy through copy_tag for the emphasis spans. %>
<p class="text-sm leading-relaxed text-ink">
  <%= copy_tag(Monte::Insight.call(scenario, results, amount: comparison.amount), currency: currency) %>
</p>
